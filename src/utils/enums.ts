export const enum UserRole {
  Poster = 'POSTER',
  Tasker = 'TASKER',
}

export const enum ContractStatus {
  PENDING = 'PENDING',
  ACCEPTED = 'ACCEPTED',
  REJECTED = 'REJECTED',
  WITHDRAWN = 'WITHDRAWN',
  CLOSED = 'CLOSED',
  COMPLETED = 'COMPLETED',
}

export const enum PaymentStatus {
  PAID = 'PAID',
  INITIATED = 'INITIATED',
  // FAILED = 'FAILED',
}